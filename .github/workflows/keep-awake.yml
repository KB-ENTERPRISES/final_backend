name: Keep Backend Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl -I https://kb-enterprises-backend-code-qirn.onrender.com/health
